# Copy this file to .env.local and fill in the values.

# ── Google Chat ─────────────────────────────────────────────────────────────
# Webhook URL for sending field notes to a Google Chat space
GOOGLE_CHAT_WEBHOOK_URL=

# ── API Key (Layer 1 — endpoint abuse protection) ───────────────────────────
# Both variables must be set to the SAME random hex string.
# Server-only version (used inside API route handlers to verify incoming requests)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FIELD_NOTES_API_KEY=

# Client-exposed version (sent as the x-api-key request header from the browser)
NEXT_PUBLIC_FIELD_NOTES_API_KEY=

# ── Auth Secret (Layer 2 — persistent password gate) ────────────────────────
# The shared password that all supervisors enter on the /login page.
# On success, a 1-year httpOnly cookie is set with this value.
# To rotate: change this value and redeploy — all existing cookies are instantly invalid.
# Generate: node -e "console.log(require('crypto').randomBytes(12).toString('base64'))"
FIELD_AUTH_SECRET=
